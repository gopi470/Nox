[package]
name = "test_winrt_spp"
version = "0.1.0"
edition = "2021"

[dependencies]
windows = { version = "0.58", features = ["Devices_Bluetooth", "Devices_Bluetooth_Rfcomm", "Networking_Sockets", "Storage_Streams", "Foundation"] }
